sudo: required
services:
  - docker
before_install:
  - openssl aes-256-cbc -K $encrypted_9f3b5599b056_key -iv $encrypted_9f3b5599b056_iv
    -in service-account.json.enc -out service-account.json -d
  - curl https://sdk.cloud.google.com > sdk-cloud-install.sh;
  - bash sdk-cloud-install.sh --disable-prompts
  - source $HOME/google-cloud-sdk/path.bash.inc
  - gcloud components install kubectl -q
  - gcloud auth activate-service-account --key-file service-account.json -q
  - gcloud config set project multi-k8s-305419 -q
  - gcloud config set compute/zone us-central1-c -q
  - gcloud container clusters get-credentials multi-cluster -q
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker build -t shumyk/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true shumyk/react-test npm test -- --coverage

deploy:
  provider: script
  script: bash ./deploy.sh
  on:
    branch: master